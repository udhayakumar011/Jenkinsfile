apiVersion: v1
kind: Service
metadata:
  name: spring-boot-cicd
  namespace: production
spec:
  selector:
    app: spring-boot-cicd
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: LoadBalancer
